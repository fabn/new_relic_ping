require 'spec_helper'

describe HeartbeatRails do
  it 'has a version number' do
    expect(HeartbeatRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
